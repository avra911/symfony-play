{% extends 'base.html.twig' %}

{% block title %}Adauga Job{% endblock %}

{% block heading %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Job
                <small>creation</small>
            </h1>
            <ol class="breadcrumb">
                <li><a href="{{ url('homepage') }}">Home</a></li>
                <li><a href="{{ path('jobs_index') }}">Jobs</a></li>
                <li class="active">New</li>
            </ol>
        </div>
    </div>
{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" type="button" class="btn btn-success pull-right">Create</button>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('jobs_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
